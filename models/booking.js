class Booking {
    constructor(user_id, experience_id, dateTime) {
        this.user_id = user_id;
        this.experience_id = experience_id;
        this.dateTime = dateTime;
    }
}

module.exports = Booking;
